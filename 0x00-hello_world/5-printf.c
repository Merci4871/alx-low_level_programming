#include <stdio.h>
/**
 * main - this is the entry point for c execution
 *
 * Return: it will return integer value
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
